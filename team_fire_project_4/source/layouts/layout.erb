<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport"
          content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Use the title from a page's frontmatter if it has one -->
    <title><%= current_page.data.title || "Tamal K Dey" %></title>
    <link rel="stylesheet" href=".<%= url_for './stylesheets/site.css' %>">
    <%= javascript_include_tag "site" %>
  </head>
  <body>
   <section id="wrapper"<% unless current_page %> class="homepage"<% end %>>
      <%= yield %>
      <footer id="footer">
        <p class="small">© <%= config[:site_author] %> <%= Time.now.year %></p>
      </footer>
    </section>
  </body>
</html>

